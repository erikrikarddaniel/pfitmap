<h1>Listing db_sequences</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @db_sequences.each do |db_sequence| %>
  <tr>
    <td><%= db_sequence.created_at %></td>
    <td><%= link_to 'Show', db_sequence %></td>
    <td><%= link_to 'Edit', edit_db_sequence_path(db_sequence) %></td>
    <td><%= link_to 'Destroy', db_sequence, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Db sequence', new_db_sequence_path %>
