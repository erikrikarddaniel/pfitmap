var el = $(<%= row_id(@parent_taxon)%>);
<% @taxons.each do |taxon| %>	
   el.after("<%=(j render(:partial => 'row', :locals => {taxon: taxon})).html_safe %>)");
<% end %>
colorHeatmap();
IndentTaxons();

el.children('.taxon').html("<%= (j link_to_collapse(@parent_taxon, @enzyme_ids, @level) )%><%= @parent_taxon.name %>");

initTips();