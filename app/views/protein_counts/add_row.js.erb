var el = $(<%= row_id(@parent_taxon)%>);
<% @taxons.each do |taxon| %>	
   el.after("<%=(j render(:partial => 'row', :locals => {taxon: taxon})).html_safe %>)");
<% end %>
colorHeatmap();
IndentTaxons();
el.children('.taxon').html("<%= (j link_to "-", { :controller => "protein_counts", :action => "collapse_rows", :parent_id => @parent_taxon.id, :level => (@level-1), :enzyme_ids => @enzyme_ids}, :remote => true)%> <%= @parent_taxon.name %>");
