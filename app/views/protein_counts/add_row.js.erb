var el = $(<%=@parent_taxon.name + @parent_taxon.id.to_s%>);
el.children('.taxon').html(<%= link_to @parent_taxon.name, { :controller => "protein_counts", :action => "colapse_row", :parent_id => @parent_taxon.id, :level => level}, :remote => true %>)
<% @taxons.each do |taxon| %>	
   el.after("<%=(j render(:partial => 'row', :locals => {taxon: taxon, level: @level})).html_safe %>)");
<% end %>
colorHeatmap();
IndentTaxons();