<ul class="nav nav-pills">
  <li class="dropdown">
    <a class="dropdown-toggle" data-toggle="dropdown" href="#">
      Select Taxon Rank
      <b class="caret" ></b>
    </a>
    <ul id="taxon_rank_menu" class="dropdown-menu">
      <% @taxon_ranks.each do |rank_name| %>
      <li>
	<%= link_to rank_name, protein_counts_with_enzymes_path(protein_counts_params({taxon_rank: rank_name}, params.dup)) %>
      </li>
      <% end %>
      <li class="divider"></li>
      <li>
	<%= link_to "All", protein_counts_with_enzymes_path(protein_counts_params({taxon_rank: "All"}, params.dup)) %>
      </li>
    </ul>
  </li>
</ul>
