<div class="heat-map">
<table class="heat-map" id="heatmap">
  <% if @enzymes %>
  <thead>
    <tr>
      <th>
	<table>
	  <thead>
	  </thead>
	  <tbody>
	    <tr>
	      <td>Taxon</td>
	    </tr>
	  </tbody>
	</table>
      </th>
      <% @parent_enzyme_ids.each do |parent_id| %>
      <%= render :partial => 'header_table', :locals => {parent_id: parent_id, enzyme_tree: @enzyme_tree, enzyme_ids: @enzyme_tree.keys} %>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% @taxons.each do |taxon| %>
    <%= render :partial => 'row', :locals => {taxon: taxon} %>
    <% end %>
  </tbody>
  <% end %>
</table>
<%= will_paginate @taxons %>
<br />
</div>
