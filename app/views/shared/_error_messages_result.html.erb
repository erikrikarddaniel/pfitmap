<% if @hmm_result.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@hmm_result.errors.count, "error") %>.
    </div>
    <ul>
    <% @hmm_result.errors.full_messages.each do |msg| %>
      <li> <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
