<%= form_for(@enzyme) do |f| %>
<%= render :partial => 'shared/error_messages', :locals => { :error_subject => @enzyme} %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  
  <div class="field">
    <%= f.label :hmm_profile %>
    <%= select_tag("hmm_profile_ids[]", options_for_select(@hmm_profiles.collect { |profile| [profile.name, profile.id] }, @enzyme.hmm_profiles.collect { |profile| profile.id}), {:multiple=> true})%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
