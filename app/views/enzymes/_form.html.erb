<%= form_for(@enzyme) do |f| %>
  <% if @enzyme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@enzyme.errors.count, "error") %> prohibited this enzyme from being saved:</h2>

      <ul>
      <% @enzyme.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
